Q1.发生异常不能保证还有result对象返回redis



实现步骤：
1.先计算完成result对象里面的baseAnalysis对象的属性
 1.1)知道总共要计算多少，要封装多少属性
 1.2)各个属性的数据封装，各个击破
 
 
 
 baseanalysis的数据封装------------------------------------【done】
/**
baseanalysis 对象统计：
微博分析(
1-转发评论数(那天，转发的多少，评论的多少,总数应该是20条)，  -------------[done]
2-用户类型（水军用户，真实用户），   -------------------------------[done]
3-表情（统计每种表情的个数），  ----------------------------------[done]
4-认证类型（四种）， -----------------------------------------[done]
5-关键词（和表情一样），---------------------------------------[done]
6.（地区，统计各个地区的个数，和表情一样），--------------------------[done]
7.(粉丝，的区间个数))  --------------------------------------[done]
*/




实现步骤：
1.先计算完成result对象里面的transmitDetails对象的属性
 1.1)知道总共要计算多少，要封装多少属性
 1.2)各个属性的数据封装，各个击破
 
 
transmitDetails的数据封装 ----------------------------[状态看下面]

String analysisBlogId;  ---------[done]
int levelId;----------------------[暂时设定为1]
String uid; ---------------------[done]
String name;---------------------[done]
String headUrl;------------------[done]
String url;----------------------[done]
int transmits;-------------------[done]
String province;-----------------[done]
String city;---------------------[done]
int posts;-----------------------[done]
int fans;------------------------[done]
String fromUid;------------------[暂时设定为源微博]
String blogId;-------------------[done]
int comments;--------------------[done]
String content;------------------[done]
String time;---------------------[done]




Q2.框架问题：
 定义代码将和逻辑代码放到一块了比如Comsumer.java
 
 
Q3.计算异常没有去充分处理


